import React from 'react';

// Containers
import { ClientContactTabContainer } from '../../containers/';

// Components
import { ContainerComponent } from '../../components/';

const ClientContactTabView = () => (
  <ContainerComponent withoutSafeAreView>
    <ClientContactTabContainer />
  </ContainerComponent>
);

export default ClientContactTabView;
